---
import { getTranslations } from '../../i18n/utils';
import type { Lang } from '../../i18n/utils';
import LanguageSwitcher from '../ui/LanguageSwitcher.astro';
const lang = (Astro.currentLocale ?? 'fr') as Lang;
const t = getTranslations(lang);
const base = lang === 'fr' ? '' : `/${lang}`;
---

<header id="site-header" class="site-header">
  <div class="section-container header-inner">
    <!-- Logo -->
    <a href={base || '/'} class="logo" aria-label="Rozi Dance — accueil">
      <img
        src="/images/logo.webp"
        alt=""
        class="logo-img"
        width="76"
        height="76"
        loading="eager"
        decoding="async"
      />
      <span class="logo-text">Rozi <span class="logo-dot">·</span> Dance</span>
    </a>

    <!-- Nav desktop -->
    <nav class="nav-desktop" aria-label="Navigation principale">
      <ul>
        <li><a href={`${base || '/'}#prestations`} class="nav-link">{t.nav.prestations}</a></li>
        <li><a href={`${base || '/'}#galerie`} class="nav-link">{t.nav.galerie}</a></li>
        <li><a href={`${base}/cours`} class="nav-link">{t.nav.cours}</a></li>
        <li><a href={`${base || '/'}#apropos`} class="nav-link">{t.nav.apropos}</a></li>
        <li><a href={`${base || '/'}#contact`} class="nav-link">{t.nav.contact}</a></li>
      </ul>
    </nav>

    <!-- CTA desktop -->
    <a href={`${base || '/'}#contact`} class="btn-primary header-cta">
      {t.nav.contact}
    </a>

    <!-- Sélecteur de langue + hamburger groupés -->
    <div class="header-end">
      <LanguageSwitcher />
      <button
        id="menu-toggle"
        class="menu-toggle"
        aria-label="Ouvrir le menu"
        aria-expanded="false"
        aria-controls="mobile-menu"
      >
        <span></span>
        <span></span>
        <span></span>
      </button>
    </div>
  </div>
</header>

<!-- Backdrop semi-transparent derrière le drawer -->
<div id="menu-backdrop" class="menu-backdrop" aria-hidden="true"></div>

<!-- Drawer mobile depuis la droite -->
<nav id="mobile-menu" class="nav-mobile" aria-label="Menu mobile" aria-hidden="true">
  <div class="mobile-menu-inner">

    <!-- Liens nav -->
    <ul>
      <li><a href={`${base || '/'}#prestations`} class="mobile-nav-link"><span class="link-text">{t.nav.prestations}</span></a></li>
      <li><a href={`${base || '/'}#galerie`} class="mobile-nav-link"><span class="link-text">{t.nav.galerie}</span></a></li>
      <li><a href={`${base}/cours`} class="mobile-nav-link"><span class="link-text">{t.nav.cours}</span></a></li>
      <li><a href={`${base || '/'}#apropos`} class="mobile-nav-link"><span class="link-text">{t.nav.apropos}</span></a></li>
      <li><a href={`${base || '/'}#contact`} class="mobile-nav-link"><span class="link-text">{t.nav.contact}</span></a></li>
    </ul>

    <div class="mobile-menu-footer">
      <span class="mobile-menu-sig">Rozi <span class="mobile-menu-dot">·</span> Dance</span>
    </div>
  </div>
</nav>

<style>
  .site-header {
    position: sticky;
    top: 0;
    z-index: 150;
    background-color: var(--color-bg-primary);
    border-bottom: 0.5px solid transparent;
    transition: border-color 300ms, backdrop-filter 300ms, background-color 300ms;
  }

  .site-header.scrolled {
    border-color: var(--color-border);
    backdrop-filter: blur(12px);
    background-color: rgba(247, 241, 230, 0.85);
  }

  /* Header ivoire quand le menu est ouvert */
  .site-header.menu-open {
    background-color: var(--color-bg-primary);
    border-bottom-color: transparent;
  }

  .header-inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    height: 72px;
    gap: 2rem;
  }

  .logo {
    text-decoration: none;
    flex-shrink: 0;
    display: flex;
    align-items: center;
    gap: 0.625rem;
  }

  .logo-img {
    display: block;
    width: 76px;
    height: 76px;
    object-fit: contain;
  }

  .logo-text {
    font-family: var(--font-serif);
    font-size: 1.375rem;
    font-weight: 400;
    color: var(--color-text-primary);
    letter-spacing: 0.02em;
  }

  .logo-dot {
    color: var(--color-accent);
  }

  .nav-desktop ul {
    display: flex;
    align-items: center;
    gap: 2rem;
    list-style: none;
    margin: 0;
    padding: 0;
  }

  .header-cta {
    flex-shrink: 0;
    padding: 0.5rem 1.25rem;
  }

  .header-end {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    flex-shrink: 0;
  }

  /* ── Hamburger ── */
  .menu-toggle {
    display: none;
    flex-direction: column;
    justify-content: center;
    gap: 5px;
    width: 32px;
    height: 32px;
    background: none;
    border: none;
    padding: 0;
    cursor: pointer;
    z-index: 1;
  }

  .menu-toggle span {
    display: block;
    width: 100%;
    height: 1px;
    background-color: var(--color-text-primary);
    transition: transform 350ms var(--ease-out-soft), opacity 250ms, width 350ms var(--ease-out-soft);
    transform-origin: center;
  }

  .menu-toggle[aria-expanded="true"] span:nth-child(1) {
    transform: translateY(6px) rotate(45deg);
  }
  .menu-toggle[aria-expanded="true"] span:nth-child(2) {
    opacity: 0;
    width: 0;
  }
  .menu-toggle[aria-expanded="true"] span:nth-child(3) {
    transform: translateY(-6px) rotate(-45deg);
  }

  /* ── Backdrop derrière le drawer ── */
  .menu-backdrop {
    position: fixed;
    inset: 0;
    z-index: 139;
    background: rgba(43, 31, 21, 0.25);
    opacity: 0;
    pointer-events: none;
    transition: opacity 400ms var(--ease-out-soft);
  }
  .menu-backdrop.is-open {
    opacity: 1;
    pointer-events: auto;
  }

  /* ── Panneau compact depuis le haut-droite ── */
  .nav-mobile {
    position: fixed;
    top: 72px;
    right: 0;
    bottom: auto;
    width: min(210px, 72vw);
    z-index: 140;
    background: rgba(251, 247, 240, 0.18);
    backdrop-filter: blur(32px) saturate(1.6);
    -webkit-backdrop-filter: blur(32px) saturate(1.6);
    box-shadow: -4px 8px 32px rgba(43, 31, 21, 0.12);
    border-radius: 0 0 0 10px;
    border: 0.5px solid rgba(168, 135, 83, 0.18);
    border-right: none;

    /* Fermé */
    transform-origin: top right;
    transform: scale(0.88) translateY(-16px);
    opacity: 0;
    pointer-events: none;
    transition: transform 500ms cubic-bezier(0.34, 1.52, 0.64, 1),
                opacity 220ms var(--ease-out-soft);
  }

  .nav-mobile.is-open {
    transform: scale(1) translateY(0);
    opacity: 1;
    pointer-events: auto;
  }

  .mobile-menu-inner {
    display: flex;
    flex-direction: column;
    gap: 0.9rem;
    padding: 1.5rem 1.5rem 1.25rem;
  }

  .nav-mobile ul {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: 0;
  }

  .mobile-nav-link {
    font-family: var(--font-serif);
    font-size: 1.15rem;
    color: rgba(43, 31, 21, 0.95);
    text-decoration: none;
    letter-spacing: 0.02em;
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 0.45rem 0;
    border-bottom: 0.5px solid rgba(193, 104, 71, 0.1);
    position: relative;

    opacity: 0;
    transform: translateX(22px);
    filter: blur(4px);
    transition: color 220ms,
                transform 320ms cubic-bezier(0.34, 1.56, 0.64, 1),
                opacity 500ms var(--ease-out-soft),
                filter 500ms var(--ease-out-soft);
  }

  .nav-mobile ul li:first-child .mobile-nav-link {
    border-top: 0.5px solid rgba(193, 104, 71, 0.1);
  }

  @keyframes textSweep {
    from { background-position: center 300%; }
    to   { background-position: center -300%; }
  }

  .link-text {
    background: linear-gradient(to bottom,
      rgba(43,31,21,0.95) 35%,
      rgba(193,104,71,1) 50%,
      rgba(43,31,21,0.95) 65%
    );
    background-size: 100% 300%;
    background-position: center 300%;
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .mobile-nav-link:hover .link-text {
    -webkit-text-fill-color: var(--color-accent);
  }

  .nav-mobile.is-open li:nth-child(1) .link-text { animation: textSweep 3000ms ease-in-out  820ms forwards; }
  .nav-mobile.is-open li:nth-child(2) .link-text { animation: textSweep 3000ms ease-in-out 1300ms forwards; }
  .nav-mobile.is-open li:nth-child(3) .link-text { animation: textSweep 3000ms ease-in-out 1780ms forwards; }
  .nav-mobile.is-open li:nth-child(4) .link-text { animation: textSweep 3000ms ease-in-out 2260ms forwards; }
  .nav-mobile.is-open li:nth-child(5) .link-text { animation: textSweep 3000ms ease-in-out 2740ms forwards; }

  .mobile-nav-link::after {
    content: '›';
    font-family: var(--font-serif);
    font-size: 1.2rem;
    color: var(--color-accent);
    opacity: 0;
    transform: translateX(-10px);
    transition: opacity 260ms var(--ease-out-soft),
                transform 320ms cubic-bezier(0.34, 1.56, 0.64, 1);
    flex-shrink: 0;
  }

  .mobile-nav-link:hover {
    color: var(--color-accent);
    transform: translateX(8px);
  }
  .mobile-nav-link:hover::after {
    opacity: 1;
    transform: translateX(0);
  }

  .nav-mobile.is-open .mobile-nav-link {
    opacity: 1;
    transform: translateX(0);
    filter: blur(0);
  }

  .nav-mobile.is-open li:nth-child(1) .mobile-nav-link { transition-delay: 80ms; }
  .nav-mobile.is-open li:nth-child(2) .mobile-nav-link { transition-delay: 130ms; }
  .nav-mobile.is-open li:nth-child(3) .mobile-nav-link { transition-delay: 180ms; }
  .nav-mobile.is-open li:nth-child(4) .mobile-nav-link { transition-delay: 230ms; }
  .nav-mobile.is-open li:nth-child(5) .mobile-nav-link { transition-delay: 280ms; }


  /* Signature en bas */
  .mobile-menu-footer {
    opacity: 0;
    transform: translateY(8px);
    transition: opacity 450ms var(--ease-out-soft) 380ms,
                transform 450ms var(--ease-out-soft) 380ms;
  }
  .nav-mobile.is-open .mobile-menu-footer {
    opacity: 1;
    transform: translateY(0);
  }


  .mobile-menu-sig {
    font-family: var(--font-serif);
    font-size: 0.875rem;
    color: rgba(43, 31, 21, 0.4);
    letter-spacing: 0.06em;
  }

  .mobile-menu-dot { color: var(--color-accent); }

  @media (max-width: 768px) {
    .nav-desktop,
    .header-cta {
      display: none;
    }

    .menu-toggle {
      display: flex;
    }
  }

  /* Sur desktop : jamais d'overlay */
  @media (min-width: 769px) {
    .nav-mobile {
      display: none !important;
    }
  }
</style>

<script>
  let headerScrollHandler: (() => void) | null = null;

  function initHeader() {
    if (headerScrollHandler) {
      window.removeEventListener('scroll', headerScrollHandler);
    }

    const header = document.getElementById('site-header');
    const toggle = document.getElementById('menu-toggle');
    const mobileMenu = document.getElementById('mobile-menu');
    const backdrop = document.getElementById('menu-backdrop');

    headerScrollHandler = () => {
      header?.classList.toggle('scrolled', window.scrollY > 40);
    };
    window.addEventListener('scroll', headerScrollHandler, { passive: true });
    headerScrollHandler();

    function openMenu() {
      mobileMenu?.classList.add('is-open');
      backdrop?.classList.add('is-open');
      mobileMenu?.setAttribute('aria-hidden', 'false');
      toggle?.setAttribute('aria-expanded', 'true');
      toggle?.setAttribute('aria-label', 'Fermer le menu');
      document.body.style.overflow = 'hidden';
    }

    function closeMenu() {
      mobileMenu?.classList.remove('is-open');
      backdrop?.classList.remove('is-open');
      mobileMenu?.setAttribute('aria-hidden', 'true');
      toggle?.setAttribute('aria-expanded', 'false');
      toggle?.setAttribute('aria-label', 'Ouvrir le menu');
      document.body.style.overflow = '';
    }

    toggle?.addEventListener('click', () => {
      const isOpen = toggle.getAttribute('aria-expanded') === 'true';
      isOpen ? closeMenu() : openMenu();
    });

    backdrop?.addEventListener('click', closeMenu);

    mobileMenu?.querySelectorAll('a').forEach((link) => {
      link.addEventListener('click', closeMenu);
    });

    // Ferme au swipe vers le haut (UX mobile)
    let touchStartY = 0;
    mobileMenu?.addEventListener('touchstart', (e) => {
      touchStartY = e.touches[0].clientY;
    }, { passive: true });
    mobileMenu?.addEventListener('touchend', (e) => {
      if (touchStartY - e.changedTouches[0].clientY > 60) closeMenu();
    }, { passive: true });
  }

  document.addEventListener('astro:page-load', initHeader);
</script>
