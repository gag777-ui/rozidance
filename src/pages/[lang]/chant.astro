---
import Layout from '../../layouts/Layout.astro';
import { getTranslations } from '../../i18n/utils';
import type { Lang } from '../../i18n/utils';

export function getStaticPaths() {
  return [
    { params: { lang: 'en' } },
    { params: { lang: 'nl' } },
    { params: { lang: 'ru' } },
  ];
}

const lang = (Astro.currentLocale ?? Astro.params.lang ?? 'fr') as Lang;
const t = getTranslations(lang);
const c = t.chant;
---

<Layout title={c.pageTitle} description={c.pageDescription}>
  <!-- Hero -->
  <section class="chant-hero">
    <div class="section-container chant-hero-inner">
      <a href={`/${lang}#prestations`} class="back-link">
        <svg width="14" height="14" viewBox="0 0 14 14" fill="none" aria-hidden="true">
          <path d="M12 7H2M6 3l-4 4 4 4" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round"/>
        </svg>
        {t.nav.prestations}
      </a>
      <span class="eyebrow">{c.heroEyebrow}</span>
      <h1>{c.heroTitle}</h1>
      <p class="hero-subtitle">{c.heroSubtitle}</p>
    </div>
  </section>

  <!-- 3 category cards -->
  <section class="categories-section">
    <div class="section-container">
      <div class="categories-grid">
        {c.categories.map((cat, i) => (
          <div
            class="category-card reveal"
            style={`transition-delay: ${i * 100}ms`}
          >
            <div class="card-accent-bar"></div>
            <div class="card-body">
              <span class="card-ordinal" aria-hidden="true">{String(i + 1).padStart(2, '0')}</span>
              <div class="card-thumb">
                <img src="/images/reel-cover.webp" alt={cat.title} loading="lazy" width="400" height="400" />
              </div>
              <h2 class="card-title">{cat.title}</h2>
              <p class="card-desc">{cat.description}</p>
              <span class="card-link">
                {c.cardLink}
                <svg width="14" height="14" viewBox="0 0 14 14" fill="none" aria-hidden="true" class="card-arrow">
                  <path d="M2 7h10M8 3l4 4-4 4" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round"/>
                </svg>
              </span>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- CTA -->
  <section class="chant-cta">
    <div class="section-container chant-cta-inner reveal-fade">
      <span class="eyebrow">{c.ctaEyebrow}</span>
      <h2>{c.ctaTitle}</h2>
      <p>{c.ctaText}</p>
      <a href={`/${lang}#contact`} class="cta-btn">
        {c.ctaBtn}
        <svg width="14" height="14" viewBox="0 0 14 14" fill="none" aria-hidden="true">
          <path d="M2 7h10M8 3l4 4-4 4" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round"/>
        </svg>
      </a>
    </div>
  </section>
</Layout>

<style>
  .chant-hero {
    padding-block: var(--spacing-section);
    background-color: var(--color-bg-section);
    border-bottom: 0.5px solid var(--color-border);
  }

  .chant-hero-inner { display: flex; flex-direction: column; gap: 1rem; }

  .back-link {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    font-size: 11px;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--color-text-secondary);
    text-decoration: none;
    margin-bottom: 0.5rem;
    transition: color 220ms var(--ease-out-soft), gap 220ms var(--ease-out-soft);
  }

  .back-link:hover { color: var(--color-accent); gap: 0.6rem; }

  .chant-hero h1 {
    font-family: var(--font-serif);
    font-size: clamp(2rem, 5vw, 3.5rem);
    font-weight: 400;
    color: var(--color-text-primary);
    line-height: 1.15;
    margin: 0;
  }

  .hero-subtitle {
    font-size: clamp(0.9375rem, 1.5vw, 1.0625rem);
    color: var(--color-text-secondary);
    line-height: 1.7;
    max-width: 56ch;
    margin: 0;
  }

  .categories-section { padding-block: var(--spacing-section); }

  .categories-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 1.25rem;
  }

  @media (min-width: 600px) { .categories-grid { grid-template-columns: repeat(3, 1fr); } }

  .category-card {
    position: relative;
    display: flex;
    flex-direction: column;
    background-color: var(--color-bg-card);
    border: 0.5px solid var(--color-border);
    border-radius: var(--radius-card);
    overflow: hidden;
    transition: transform 400ms var(--ease-out-soft), border-color 400ms var(--ease-out-soft), box-shadow 400ms var(--ease-out-soft);
  }

  .category-card:hover {
    transform: translateY(-4px);
    border-color: var(--color-accent);
    box-shadow: 0 12px 32px rgba(193, 104, 71, 0.12);
  }

  .card-accent-bar {
    height: 3px;
    background: linear-gradient(to right, var(--color-accent), var(--color-gold));
    flex-shrink: 0;
  }

  .card-body {
    padding: 1.5rem;
    display: flex;
    flex-direction: column;
    gap: 0.875rem;
    flex: 1;
    position: relative;
  }

  .card-ordinal {
    position: absolute;
    top: 1rem;
    right: 1.25rem;
    font-family: var(--font-serif);
    font-size: 3rem;
    line-height: 1;
    color: var(--color-border);
    pointer-events: none;
    user-select: none;
    transition: color 400ms var(--ease-out-soft);
  }

  .category-card:hover .card-ordinal { color: rgba(193, 104, 71, 0.1); }

  .card-thumb {
    width: 56px;
    height: 56px;
    border-radius: 50%;
    overflow: hidden;
    border: 0.5px solid var(--color-border);
    flex-shrink: 0;
  }

  .card-thumb img { width: 100%; height: 100%; object-fit: cover; display: block; }

  .card-title {
    font-family: var(--font-serif);
    font-size: 1.2rem;
    font-weight: 400;
    color: var(--color-text-primary);
    margin: 0;
    line-height: 1.3;
  }

  .card-desc { font-size: 0.9rem; color: var(--color-text-secondary); line-height: 1.6; margin: 0; flex: 1; }

  .card-link {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 11px;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--color-accent);
    margin-top: 0.25rem;
    transition: gap 250ms var(--ease-out-soft);
  }

  .category-card:hover .card-link { gap: 0.75rem; }

  .card-arrow { flex-shrink: 0; transition: transform 250ms var(--ease-out-soft); }
  .category-card:hover .card-arrow { transform: translateX(3px); }

  .chant-cta {
    padding-block: var(--spacing-section);
    background-color: var(--color-bg-section);
    border-top: 0.5px solid var(--color-border);
  }

  .chant-cta-inner {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    gap: 1.25rem;
  }

  .chant-cta h2 {
    font-family: var(--font-serif);
    font-size: clamp(1.75rem, 3.5vw, 2.75rem);
    font-weight: 400;
    color: var(--color-text-primary);
    margin: 0;
  }

  .chant-cta p { font-size: 1rem; color: var(--color-text-secondary); line-height: 1.7; max-width: 50ch; margin: 0; }

  .cta-btn {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.75rem 1.75rem;
    background-color: var(--color-accent);
    color: white;
    font-size: 12px;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    text-decoration: none;
    border-radius: var(--radius-btn);
    transition: background-color 300ms var(--ease-out-soft), gap 250ms var(--ease-out-soft);
    margin-top: 0.5rem;
  }

  .cta-btn:hover { background-color: color-mix(in srgb, var(--color-accent) 85%, black); gap: 0.75rem; }
</style>
