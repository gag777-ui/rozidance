---
import LegalPage from '../mentions-legales.astro';

export function getStaticPaths() {
  return ['en', 'nl', 'ru', 'de', 'es', 'it'].map((lang) => ({
    params: { lang },
  }));
}
---

<LegalPage />
