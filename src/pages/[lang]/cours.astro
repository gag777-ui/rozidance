---
import Layout from '../../layouts/Layout.astro';
import { getTranslations } from '../../i18n/utils';
import type { Lang } from '../../i18n/utils';
import CtaFloat from '../../components/ui/CtaFloat.astro';

export function getStaticPaths() {
  return [
    { params: { lang: 'en' } },
    { params: { lang: 'nl' } },
    { params: { lang: 'ru' } },
    { params: { lang: 'de' } },
    { params: { lang: 'es' } },
    { params: { lang: 'it' } },
  ];
}

const lang = (Astro.currentLocale ?? Astro.params.lang ?? 'fr') as Lang;
const t = getTranslations(lang);
const faq = t.cours.faqItems;
// Reels are language-agnostic — always read from fr.json
const fr = (await import('../../i18n/fr.json')).default;
const reels = (fr.cours as any).reels as (string | null)[] ?? [];
---

<Layout title={t.cours.pageTitle} description={t.cours.pageDescription}>
  <!-- Hero -->
  <section class="cours-hero" aria-labelledby="cours-title">
    <div class="section-container cours-hero-inner">
      <div class="cours-hero-text">
        <a href={`/${lang}#prestations`} class="back-link">
          <svg width="16" height="16" viewBox="0 0 14 14" fill="none" aria-hidden="true">
            <path d="M12 7H2M6 3l-4 4 4 4" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
          {t.nav.prestations}
        </a>
        <span class="eyebrow">{t.cours.heroEyebrow}</span>
        <h1 id="cours-title">{t.cours.heroTitle}</h1>
        <p class="cours-hero-subtitle">{t.cours.heroSubtitle}</p>
      </div>
      <!-- Desktop: Carousel classique (original) -->
      <div class="cours-carousel" id="cours-carousel" aria-label="Aperçu cours de chant et danse">
        {[
          { src: '/videos/cours/cours-1.mp4', href: 'https://www.instagram.com/reel/DPgmkq3iug0/', start: 5  },
          { src: '/videos/cours/cours-2.mp4', href: 'https://www.instagram.com/reel/C1M02FgIz6j/', start: 25 },
          { src: '/videos/cours/cours-3.mp4', href: 'https://www.instagram.com/reel/DLrwoH9ojVe/', start: 10 },
          { src: '/videos/cours/cours-4.mp4', href: 'https://www.instagram.com/reel/C_6LDgrIsAA/', start: 8  },
          { src: '/videos/cours/cours-5.mp4', href: 'https://www.instagram.com/reel/DH1h3BnI9XE/', start: 8  },
        ].map((v, i) => (
          <div class={`coc-slide${i === 0 ? ' is-active' : ''}`} data-start={v.start}>
            <a href={v.href} target="_blank" rel="noopener noreferrer" class="coc-link"
              aria-label={`Voir le reel ${i + 1} sur Instagram`} tabindex={i === 0 ? 0 : -1}>
              <video class="coc-video" muted loop playsinline disablepictureinpicture preload="none">
                <source src={v.src} type="video/mp4" />
              </video>
              <div class="coc-overlay" aria-hidden="true">
                <svg width="15" height="15" viewBox="0 0 24 24" fill="currentColor">
                  <path d="M12 2.163c3.204 0 3.584.012 4.85.07 3.252.148 4.771 1.691 4.919 4.919.058 1.265.069 1.645.069 4.849 0 3.205-.012 3.584-.069 4.849-.149 3.225-1.664 4.771-4.919 4.919-1.266.058-1.644.07-4.85.07-3.204 0-3.584-.012-4.849-.07-3.26-.149-4.771-1.699-4.919-4.92-.058-1.265-.07-1.644-.07-4.849 0-3.204.013-3.583.07-4.849.149-3.227 1.664-4.771 4.919-4.919 1.266-.057 1.645-.069 4.849-.069zM12 0C8.741 0 8.333.014 7.053.072 2.695.272.273 2.69.073 7.052.014 8.333 0 8.741 0 12c0 3.259.014 3.668.072 4.948.2 4.358 2.618 6.78 6.98 6.98C8.333 23.986 8.741 24 12 24c3.259 0 3.668-.014 4.948-.072 4.354-.2 6.782-2.618 6.979-6.98.059-1.28.073-1.689.073-4.948 0-3.259-.014-3.667-.072-4.947-.196-4.354-2.617-6.78-6.979-6.98C15.668.014 15.259 0 12 0zm0 5.838a6.162 6.162 0 100 12.324 6.162 6.162 0 000-12.324zM12 16a4 4 0 110-8 4 4 0 010 8zm6.406-11.845a1.44 1.44 0 100 2.881 1.44 1.44 0 000-2.881z"/>
                </svg>
                Instagram
              </div>
            </a>
          </div>
        ))}
      </div>

      <!-- Mobile: Horizontal Scroll Carousel (3D cards) -->
      <div class="video-carousel-wrapper" id="video-carousel-mobile" aria-label="Aperçu cours de chant et danse">
        <div class="video-carousel" aria-label="Vidéos cours">
          {[
            { src: '/videos/cours/cours-1.mp4',    poster: '/images/video-posters/cours-cours-1.png', href: 'https://www.instagram.com/reel/DPgmkq3iug0/', start: 5  },
            { src: '/videos/cours/cours-2.mp4',    poster: '/images/video-posters/cours-cours-2.png', href: 'https://www.instagram.com/reel/C1M02FgIz6j/', start: 25 },
            { src: '/videos/cours/cours-3.mp4',    poster: '/images/video-posters/cours-cours-3.png', href: 'https://www.instagram.com/reel/DLrwoH9ojVe/', start: 10 },
            { src: '/videos/cours/cours-4.mp4',    poster: '/images/video-posters/cours-cours-4.png', href: 'https://www.instagram.com/reel/C_6LDgrIsAA/', start: 8  },
            { src: '/videos/cours/cours-5.mp4',    poster: '/images/video-posters/cours-cours-5.png', href: 'https://www.instagram.com/reel/DH1h3BnI9XE/', start: 8  },
          ].map((v, i) => (
            <div class="video-card" data-index={i} data-start={v.start} data-src={v.src}>
              <div class="card-inner">
                <video class="video-player" muted loop playsinline disablepictureinpicture preload="metadata" poster={v.poster}>
                  <source src={v.src} type="video/mp4" />
                </video>
                <button class="play-btn" aria-label={`Jouer vidéo ${i + 1}`}>
                  <svg width="48" height="48" viewBox="0 0 24 24" fill="white">
                    <path d="M8 5v14l11-7z"/>
                  </svg>
                </button>
                <a href={v.href} class="insta-link" target="_blank" rel="noopener noreferrer" aria-label={`Voir le reel ${i + 1}`}>
                  <svg width="20" height="20" viewBox="0 0 24 24" fill="white">
                    <path d="M12 2.163c3.204 0 3.584.012 4.85.07 3.252.148 4.771 1.691 4.919 4.919.058 1.265.069 1.645.069 4.849 0 3.205-.012 3.584-.069 4.849-.149 3.225-1.664 4.771-4.919 4.919-1.266.058-1.644.07-4.85.07-3.204 0-3.584-.012-4.849-.07-3.26-.149-4.771-1.699-4.919-4.92-.058-1.265-.07-1.644-.07-4.849 0-3.204.013-3.583.07-4.849.149-3.227 1.664-4.771 4.919-4.919 1.266-.057 1.645-.069 4.849-.069zM12 0C8.741 0 8.333.014 7.053.072 2.695.272.273 2.69.073 7.052.014 8.333 0 8.741 0 12c0 3.259.014 3.668.072 4.948.2 4.358 2.618 6.78 6.98 6.98C8.333 23.986 8.741 24 12 24c3.259 0 3.668-.014 4.948-.072 4.354-.2 6.782-2.618 6.979-6.98.059-1.28.073-1.689.073-4.948 0-3.259-.014-3.667-.072-4.947-.196-4.354-2.617-6.78-6.979-6.98C15.668.014 15.259 0 12 0zm0 5.838a6.162 6.162 0 100 12.324 6.162 6.162 0 000-12.324zM12 16a4 4 0 110-8 4 4 0 010 8zm6.406-11.845a1.44 1.44 0 100 2.881 1.44 1.44 0 000-2.881z"/>
                  </svg>
                </a>
              </div>
            </div>
          ))}
        </div>
      </div>
    </div>
  </section>

  <!-- Reels grid -->
  {reels.length > 0 && (
    <section class="reels-section">
      <div class="section-container">
        <div class="reels-grid">
          {reels.map((href, i) => (
            <a
              href={href ?? '#'}
              class="reel-card reveal"
              style={`transition-delay: ${i * 50}ms`}
              aria-label={`Cours — voir le reel Instagram ${i + 1}`}
              target={href ? '_blank' : undefined}
              rel={href ? 'noopener noreferrer' : undefined}
            >
              <div class="reel-thumb">
                <img src="/images/reel-mariages.webp" alt={`Cours ${i + 1}`} loading="lazy" width="1013" height="1372" />
                <div class="reel-overlay">
                  <svg class="reel-ig-icon" width="32" height="32" viewBox="0 0 24 24" fill="none" aria-hidden="true">
                    <rect x="2" y="2" width="20" height="20" rx="5" stroke="white" stroke-width="1.4"/>
                    <circle cx="12" cy="12" r="3.5" stroke="white" stroke-width="1.4"/>
                    <circle cx="17" cy="7" r="1.2" fill="white"/>
                  </svg>
                </div>
              </div>
              <p class="reel-label">{t.nav.reelLabel}
                <svg width="10" height="10" viewBox="0 0 24 24" fill="none" aria-hidden="true">
                  <path d="M7 17L17 7M17 7H7M17 7v10" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                </svg>
              </p>
            </a>
          ))}
        </div>
      </div>
    </section>
  )}

  <!-- Section chant -->
  <section class="cours-section" aria-labelledby="chant-title">
    <div class="section-container cours-section-inner">
      <div class="cours-text reveal">
        <h2 id="chant-title">{t.cours.chantTitle}</h2>
        <p>{t.cours.chantDescription}</p>
        <ul class="cours-list">
          {t.cours.chantList.map(item => <li>{item}</li>)}
        </ul>
      </div>
      <div class="cours-deco reveal" aria-hidden="true">
        <div class="cours-deco-circle"></div>
      </div>
    </div>
  </section>

  <!-- Section danse -->
  <section class="cours-section cours-section-alt" aria-labelledby="danse-title">
    <div class="section-container cours-section-inner reverse">
      <div class="cours-deco reveal" aria-hidden="true">
        <div class="cours-deco-circle danse-circle"></div>
      </div>
      <div class="cours-text reveal">
        <h2 id="danse-title">{t.cours.danseTitle}</h2>
        <p>{t.cours.danseDescription}</p>
        <ul class="cours-list">
          {t.cours.danseList.map(item => <li>{item}</li>)}
        </ul>
      </div>
    </div>
  </section>

  <!-- FAQ -->
  <section class="cours-faq" aria-labelledby="faq-title">
    <div class="section-container">
      <div class="section-header reveal">
        <span class="eyebrow">{t.cours.faqEyebrow}</span>
        <h2 id="faq-title">{t.cours.faqTitle}</h2>
      </div>
      <div class="faq-list" id="faq-accordion">
        {faq.map((item: { q: string; a: string }, i: number) => (
          <details class="faq-item reveal" style={`transition-delay: ${i * 40}ms`}>
            <summary class="faq-question">
              {item.q}
              <span class="faq-chevron" aria-hidden="true">+</span>
            </summary>
            <p class="faq-answer">{item.a}</p>
          </details>
        ))}
      </div>
    </div>
  </section>

  <!-- CTA -->
  <section class="cours-cta" aria-labelledby="cta-title">
    <div class="section-container">
      <div class="cta-inner reveal">
        <h2 id="cta-title">{t.cours.ctaTitle}</h2>
        <p>{t.cours.ctaSubtitle}</p>
        <a href="tel:+32486219896" class="btn-primary">{t.cours.ctaBtn}</a>
      </div>
    </div>
  </section>
  <CtaFloat href="tel:+32486219896" label={t.hero.ctaPrimary} />
</Layout>

<style>
  /* Hero */
  .cours-hero {
    padding-block: 5rem 3.5rem;
    background-color: var(--color-bg-card);
    overflow: hidden;
  }

  .cours-hero-inner {
    display: grid;
    grid-template-columns: 1fr 1fr;
    align-items: center;
    gap: 2rem;
  }

  .cours-hero-text {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .cours-hero-img {
    position: relative;
  }

  .cours-hero-img img {
    width: 100%;
    height: auto;
    display: block;
    mask-image:
      linear-gradient(to right, transparent 0%, black 15%, black 90%, transparent 100%),
      linear-gradient(to bottom, transparent 0%, black 5%, black 95%, transparent 100%);
    mask-composite: intersect;
    -webkit-mask-image:
      linear-gradient(to right, transparent 0%, black 15%, black 90%, transparent 100%),
      linear-gradient(to bottom, transparent 0%, black 5%, black 95%, transparent 100%);
    -webkit-mask-composite: source-in;
  }

  @media (max-width: 768px) {
    .cours-hero-inner {
      grid-template-columns: 1fr;
    }

    .cours-hero-img {
      max-height: 220px;
      overflow: hidden;
    }

    .cours-hero-img img {
      object-fit: cover;
      object-position: center top;
      height: 220px;
      width: 100%;
    }
  }

  .back-link {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.8rem;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    color: var(--color-text-secondary);
    text-decoration: none;
    margin-bottom: 0.25rem;
    min-height: 44px;
    padding: 0.5rem 0;
    transition: color 220ms var(--ease-out-soft), gap 220ms var(--ease-out-soft);
    -webkit-tap-highlight-color: transparent;
  }
  .back-link:hover { color: var(--color-accent); gap: 0.75rem; }

  .cours-hero-inner h1 {
    font-size: clamp(2.5rem, 5vw, 4rem);
    color: var(--color-section-title);
    margin: 0;
  }

  .cours-hero-subtitle {
    font-size: 1.0625rem;
    color: var(--color-text-secondary);
    line-height: 1.65;
    margin: 0;
  }

  .reels-section { padding-block: var(--spacing-section); }

  .reels-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(140px, 1fr));
    gap: 1rem;
  }
  @media (min-width: 640px) { .reels-grid { grid-template-columns: repeat(auto-fill, minmax(170px, 1fr)); gap: 1.25rem; } }
  @media (min-width: 1024px) { .reels-grid { grid-template-columns: repeat(auto-fill, minmax(200px, 1fr)); } }

  .reel-card { display: block; text-decoration: none; }

  .reel-thumb {
    position: relative;
    aspect-ratio: 3 / 4;
    border-radius: 1.5rem;
    overflow: hidden;
    background-color: var(--color-bg-card);
    border: 0.5px solid var(--color-border);
  }

  .reel-thumb img { width: 100%; height: 100%; object-fit: cover; object-position: center; display: block; position: absolute; inset: 0; transition: transform 500ms var(--ease-out-soft); }

  .reel-overlay {
    position: absolute; inset: 0;
    display: flex; align-items: center; justify-content: center;
    background: rgba(0,0,0,0.22);
    opacity: 0;
    transition: opacity 300ms var(--ease-out-soft);
  }

  .reel-ig-icon { filter: drop-shadow(0 2px 8px rgba(0,0,0,0.5)); }
  .reel-card:hover .reel-thumb img { transform: scale(1.03); }
  .reel-card:hover .reel-overlay { opacity: 1; }

  .reel-label {
    display: flex; align-items: center; justify-content: center; gap: 0.3rem;
    margin: 0.5rem 0 0; font-size: 0.7rem; letter-spacing: 0.08em;
    text-transform: uppercase; color: var(--color-text-secondary);
    transition: color 250ms var(--ease-out-soft);
  }
  .reel-card:hover .reel-label { color: var(--color-accent); }

  .cours-section { padding-block: 5rem; }
  .cours-section-alt { background-color: var(--color-bg-card); }

  .cours-section-inner {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
  }

  .cours-section-inner.reverse { direction: rtl; }
  .cours-section-inner.reverse > * { direction: ltr; }

  .cours-text h2 { font-size: clamp(1.75rem, 3vw, 2.5rem); color: var(--color-text-primary); margin: 0 0 1.25rem; }
  .cours-text p { font-size: 1rem; color: var(--color-text-secondary); line-height: 1.75; margin: 0 0 1.5rem; }

  .cours-list {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 0.625rem;
  }

  .cours-list li {
    font-size: 0.9375rem;
    color: var(--color-text-secondary);
    padding-left: 1.25rem;
    position: relative;
  }

  .cours-list li::before {
    content: '·';
    position: absolute;
    left: 0;
    color: var(--color-accent);
    font-size: 1.25rem;
    line-height: 1;
    top: 0.1em;
  }

  .cours-deco-circle {
    width: 320px;
    height: 320px;
    border-radius: 50%;
    background: radial-gradient(circle, var(--color-gold) 0%, transparent 70%);
    opacity: 0.06;
    margin-inline: auto;
  }

  .danse-circle { background: radial-gradient(circle, var(--color-accent) 0%, transparent 70%); }

  .section-header { text-align: center; margin-bottom: 3rem; display: flex; flex-direction: column; align-items: center; gap: 0.75rem; }
  .section-header h2 { font-size: clamp(1.875rem, 3.5vw, 2.75rem); color: var(--color-text-primary); margin: 0; }

  .cours-faq { padding-block: 5rem; background-color: var(--color-bg-card); }

  .faq-list { max-width: 800px; margin-inline: auto; display: flex; flex-direction: column; gap: 0; }
  .faq-item { border-bottom: 0.5px solid var(--color-border); }

  .faq-question {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    padding: 1.25rem 0;
    font-family: var(--font-serif);
    font-size: 1.0625rem;
    color: var(--color-text-primary);
    cursor: pointer;
    list-style: none;
    user-select: none;
  }

  .faq-question::-webkit-details-marker { display: none; }

  .faq-chevron { font-size: 1.25rem; color: var(--color-accent); transition: transform 300ms var(--ease-out-soft); flex-shrink: 0; }
  details[open] .faq-chevron { transform: rotate(45deg); }

  .faq-answer { font-size: 0.9375rem; color: var(--color-text-secondary); line-height: 1.75; margin: 0; padding-bottom: 1.25rem; padding-right: 2rem; }

  .cours-cta { padding-block: 5rem; }

  .cta-inner { text-align: center; display: flex; flex-direction: column; align-items: center; gap: 1.25rem; }
  .cta-inner h2 { font-size: clamp(1.75rem, 3vw, 2.5rem); color: var(--color-text-primary); margin: 0; }
  .cta-inner p { font-size: 1rem; color: var(--color-text-secondary); margin: 0; max-width: 45ch; line-height: 1.65; }

  /* Harmonisation avec la page Mariages & Evenements */
  .cours-hero {
    position: relative;
    padding-block: clamp(4rem, 8vw, 7rem) clamp(5rem, 9vw, 8rem);
    background:
      radial-gradient(ellipse at 11% 16%, rgba(255, 255, 255, 0.72), transparent 38%),
      radial-gradient(ellipse at 90% 30%, rgba(168, 135, 83, 0.13), transparent 34%),
      linear-gradient(180deg, #fffaf3 0%, #fff3e6 58%, #f9ead7 100%);
  }

  .cours-hero::before,
  .cours-hero::after {
    content: '';
    position: absolute;
    pointer-events: none;
  }

  .cours-hero::before {
    left: 0;
    top: 12%;
    width: min(420px, 46vw);
    height: min(420px, 46vw);
    border: 0.5px solid rgba(168, 135, 83, 0.13);
    border-left: 0;
    border-radius: 0 999px 999px 0;
    opacity: 0.72;
  }

  .cours-hero::after {
    inset-inline: 0;
    bottom: -1px;
    z-index: 1;
    height: clamp(5rem, 12vw, 9rem);
    background: linear-gradient(180deg, rgba(244, 222, 196, 0), #f9ead7 62%, #fbf4ea 100%);
  }

  .cours-hero-inner {
    position: relative;
    z-index: 2;
    grid-template-columns: minmax(0, 0.95fr) minmax(300px, 1.05fr);
    gap: clamp(2rem, 5vw, 5rem);
  }

  .cours-hero-img {
    justify-self: end;
    width: min(100%, 590px);
  }

  .cours-hero-img::before {
    content: '';
    position: absolute;
    inset: 8% -4% -7% 13%;
    border-radius: 32px;
    background: linear-gradient(145deg, rgba(168, 135, 83, 0.2), rgba(255, 255, 255, 0.08));
    transform: rotate(-2deg);
  }

  .cours-hero-img::after {
    content: '';
    position: absolute;
    inset: 7% 8% 7% -6%;
    border: 0.5px solid rgba(168, 135, 83, 0.28);
    border-radius: 28px;
    transform: rotate(2.4deg);
    pointer-events: none;
  }

  .cours-hero-img img {
    position: relative;
    z-index: 1;
    border-radius: 26px;
    box-shadow: 0 24px 58px rgba(72, 46, 25, 0.13);
    -webkit-mask-image:
      linear-gradient(to right, transparent 0%, black 12%, black 94%, transparent 100%),
      linear-gradient(to bottom, transparent 0%, black 4%, black 94%, transparent 100%);
    mask-image:
      linear-gradient(to right, transparent 0%, black 12%, black 94%, transparent 100%),
      linear-gradient(to bottom, transparent 0%, black 4%, black 94%, transparent 100%);
  }

  .cours-hero-inner h1 {
    font-family: var(--font-serif);
    font-size: clamp(2.6rem, 7vw, 5.7rem);
    font-weight: 300;
    line-height: 0.95;
    max-width: 10ch;
  }

  .reels-section {
    position: relative;
    overflow: hidden;
    margin-top: -1px;
    padding-block: clamp(3.8rem, 8vw, 6.8rem) clamp(6rem, 11vw, 9rem);
    background:
      radial-gradient(ellipse at 16% 8%, rgba(168, 135, 83, 0.08), transparent 34%),
      radial-gradient(ellipse at 88% 92%, rgba(135, 144, 115, 0.07), transparent 36%),
      linear-gradient(180deg, #fbf4ea 0%, #fff8ee 44%, #f7ead9 100%);
  }

  .reels-section::after {
    content: '';
    position: absolute;
    inset-inline: 0;
    bottom: -1px;
    height: clamp(5rem, 12vw, 9rem);
    pointer-events: none;
    background: linear-gradient(180deg, rgba(242, 223, 201, 0), #faefe2 56%, #fff8ee 100%);
  }

  .reels-grid {
    position: relative;
    z-index: 1;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: clamp(0.72rem, 2.5vw, 1.1rem);
  }

  .reel-card {
    position: relative;
    display: grid;
    min-height: 188px;
    padding: 1rem;
    overflow: hidden;
    color: var(--color-charcoal);
    border: 0.5px solid rgba(154, 119, 72, 0.16);
    border-radius: 24px;
    background:
      radial-gradient(circle at 18% 14%, rgba(255, 255, 255, 0.74), transparent 28%),
      linear-gradient(145deg, rgba(255, 253, 248, 0.96), rgba(246, 235, 219, 0.82));
    box-shadow:
      0 14px 32px rgba(72, 46, 25, 0.065),
      inset 0 1px 0 rgba(255, 255, 255, 0.82);
    transition: transform 420ms var(--ease-out-soft), box-shadow 420ms var(--ease-out-soft);
  }

  .reel-card::after {
    content: '';
    position: absolute;
    right: -38px;
    top: -48px;
    width: 138px;
    height: 168px;
    border-radius: 999px;
    background: rgba(168, 135, 83, 0.1);
    transform: rotate(-24deg);
    transition: transform 650ms var(--ease-out-soft);
  }

  .reel-thumb {
    position: relative;
    z-index: 1;
    width: 44px;
    height: 44px;
    aspect-ratio: auto;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    border-color: rgba(168, 135, 83, 0.2);
    border-radius: 15px;
    background: rgba(255, 250, 241, 0.72);
    backdrop-filter: blur(10px);
  }

  .reel-thumb img,
  .reel-overlay {
    display: none;
  }

  .reel-thumb::before {
    content: '';
    width: 18px;
    height: 18px;
    border: 1.35px solid rgba(92, 64, 41, 0.78);
    border-radius: 5px;
    box-shadow: inset 0 0 0 4px rgba(255, 250, 241, 0.72);
  }

  .reel-label {
    position: absolute;
    left: 1rem;
    right: 1rem;
    bottom: 1rem;
    z-index: 1;
    justify-content: space-between;
    margin: 0;
    color: rgba(107, 86, 69, 0.78);
    font-weight: 600;
  }

  .reel-card:hover {
    transform: translateY(-4px);
    box-shadow:
      0 18px 42px rgba(72, 46, 25, 0.1),
      inset 0 1px 0 rgba(255, 255, 255, 0.88);
  }

  .reel-card:hover::after {
    transform: rotate(-16deg) translate(-8px, 8px);
  }

  .cours-section,
  .cours-section-alt,
  .cours-faq,
  .cours-cta {
    position: relative;
    overflow: hidden;
    margin-top: -1px;
    border: 0;
  }

  .cours-section {
    background:
      radial-gradient(ellipse at 14% 22%, rgba(168, 135, 83, 0.07), transparent 32%),
      linear-gradient(180deg, #fff8ee 0%, #fbf1e1 100%);
  }

  .cours-section-alt {
    background:
      radial-gradient(ellipse at 86% 18%, rgba(135, 144, 115, 0.08), transparent 34%),
      linear-gradient(180deg, #fbf1e1 0%, #f7e4cd 100%);
  }

  .cours-faq {
    background:
      radial-gradient(ellipse at 50% 0%, rgba(255, 255, 255, 0.65), transparent 34%),
      linear-gradient(180deg, #f7e4cd 0%, #fff8ee 100%);
  }

  .cours-cta {
    padding-block: clamp(4rem, 8vw, 6.6rem);
    background:
      radial-gradient(ellipse at 50% 0%, rgba(255, 255, 255, 0.72), transparent 34%),
      radial-gradient(ellipse at 12% 90%, rgba(168, 135, 83, 0.08), transparent 32%),
      linear-gradient(180deg, #fff8ee 0%, #fbf1e1 56%, #faecd9 100%);
  }

  .cours-text h2,
  .section-header h2,
  .cta-inner h2 {
    font-family: var(--font-serif);
    font-weight: 300;
    line-height: 0.98;
  }

  .faq-item {
    border-bottom-color: rgba(168, 135, 83, 0.18);
  }

  .btn-primary {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 48px;
    padding: 0.78rem 1.35rem;
    color: #fff7ec;
    border: 0.5px solid rgba(255, 234, 199, 0.32);
    border-radius: 999px;
    background: linear-gradient(145deg, rgba(92, 64, 41, 0.96), rgba(122, 92, 58, 0.94));
    box-shadow: 0 16px 32px rgba(72, 46, 25, 0.14), inset 0 1px 0 rgba(255, 255, 255, 0.18);
    font-size: 12px;
    font-weight: 600;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    transition: transform 280ms var(--ease-out-soft), background 280ms var(--ease-out-soft), box-shadow 280ms var(--ease-out-soft);
  }

  .btn-primary:hover {
    transform: translateY(-2px);
    background: linear-gradient(145deg, rgba(105, 73, 46, 0.98), rgba(137, 103, 65, 0.95));
  }

  @media (max-width: 900px) {
    .cours-section-inner { grid-template-columns: 1fr; gap: 2rem; }
    .cours-section-inner.reverse { direction: ltr; }
    .cours-deco { display: none; }
  }

  @media (max-width: 768px) {
    .cours-hero {
      padding-block: 3.2rem 4.9rem;
    }

    .cours-hero-inner {
      grid-template-columns: 1fr;
      gap: 1.6rem;
    }

    .cours-hero-img {
      justify-self: stretch;
      width: 100%;
      max-height: 260px;
      border-radius: 22px;
    }

    .cours-hero-img::before,
    .cours-hero-img::after {
      display: none;
    }

    .cours-hero-img img {
      height: 260px;
      border-radius: 22px;
    }

    .reels-grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
  }

  .cours-carousel {
    position: relative;
    isolation: isolate;
    width: min(100%, 420px);
    aspect-ratio: 3 / 4;
    margin-inline: auto;
    padding: clamp(8px, 1.3vw, 12px);
    border-radius: 36px;
    overflow: visible;
    background:
      radial-gradient(circle at 18% 12%, rgba(255, 255, 255, 0.62), transparent 30%),
      linear-gradient(145deg, rgba(255, 253, 248, 0.72), rgba(236, 216, 190, 0.32));
    touch-action: pan-y;
    box-shadow:
      0 26px 58px rgba(72, 46, 25, 0.12),
      inset 0 1px 0 rgba(255, 255, 255, 0.62);
  }

  .cours-carousel::before,
  .cours-carousel::after {
    content: '';
    position: absolute;
    pointer-events: none;
  }

  .cours-carousel::before {
    inset: -9% -12% -11% 10%;
    z-index: -1;
    border-radius: 44px;
    background:
      radial-gradient(ellipse at 70% 18%, rgba(168, 135, 83, 0.2), transparent 35%),
      radial-gradient(ellipse at 18% 86%, rgba(135, 144, 115, 0.12), transparent 38%),
      linear-gradient(145deg, rgba(168, 135, 83, 0.14), rgba(255, 255, 255, 0));
    filter: blur(0.2px);
    transform: rotate(-2.2deg);
  }

  .cours-carousel::after {
    inset: 0;
    z-index: 8;
    border: 0.5px solid rgba(168, 135, 83, 0.24);
    border-radius: inherit;
    box-shadow:
      inset 0 0 0 1px rgba(255, 255, 255, 0.26),
      inset 0 -80px 90px rgba(55, 35, 22, 0.18),
      inset 0 80px 90px rgba(255, 248, 238, 0.16);
  }

  .coc-slide {
    position: absolute;
    inset: clamp(8px, 1.3vw, 12px);
    overflow: hidden;
    border-radius: 28px;
    opacity: 0;
    transition: opacity 900ms ease-in-out;
    will-change: opacity;
  }

  .coc-slide.is-active { opacity: 1; }

  .coc-link {
    display: block;
    width: 100%;
    height: 100%;
    position: relative;
    overflow: hidden;
    border-radius: inherit;
    background: #2b1f15;
    text-decoration: none;
  }

  .coc-link::before,
  .coc-link::after {
    content: '';
    position: absolute;
    inset: 0;
    z-index: 2;
    pointer-events: none;
  }

  .coc-link::before {
    background:
      linear-gradient(90deg, rgba(35, 21, 13, 0.32), transparent 18%, transparent 82%, rgba(35, 21, 13, 0.28)),
      linear-gradient(180deg, rgba(255, 245, 231, 0.12), transparent 22%, transparent 64%, rgba(35, 21, 13, 0.34));
    mix-blend-mode: multiply;
  }

  .coc-link::after {
    inset: 10px;
    border: 0.5px solid rgba(255, 242, 219, 0.24);
    border-radius: 22px;
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.18),
      0 0 44px rgba(255, 240, 215, 0.08);
  }

  .coc-video { width: 100%; height: 100%; object-fit: cover; object-position: center 30%; display: block; }
  .coc-overlay {
    position: absolute; inset: 0; z-index: 4; display: flex; align-items: flex-end; gap: 0.5rem; padding: 1rem;
    background: linear-gradient(to top, rgba(20,10,5,0.72) 0%, transparent 55%);
    color: #fff; font-family: var(--font-sans); font-size: 11px;
    letter-spacing: 0.12em; text-transform: uppercase;
    opacity: 0; transition: opacity 300ms; pointer-events: none;
  }
  .coc-slide.is-active .coc-link:hover .coc-overlay { opacity: 1; }
  @media (max-width: 720px) {
    .cours-carousel {
      height: unset;
      min-height: unset;
      width: 75vw;
      aspect-ratio: 3 / 4;
      margin-inline: auto;
      margin-top: 1.2rem;
      padding: 7px;
      border-radius: 26px;
    }

    .cours-carousel::before {
      inset: -7% -9% -10% 9%;
      border-radius: 32px;
    }

    .coc-slide {
      inset: 7px;
      border-radius: 21px;
    }

    .coc-link::after {
      inset: 7px;
      border-radius: 17px;
    }
  }
  @media (prefers-reduced-motion: reduce) { .coc-slide { transition: none; } }

  .video-carousel-wrapper {
    position: relative;
    justify-self: center;
    width: min(100vw, 760px);
    max-width: none;
    overflow: visible;
    isolation: isolate;
    perspective: 1400px;
    perspective-origin: 50% 42%;
  }

  .video-carousel-wrapper::before {
    content: '';
    position: absolute;
    left: 50%;
    top: 50%;
    z-index: 0;
    width: min(88vw, 510px);
    height: min(74vw, 330px);
    pointer-events: none;
    border-radius: 56% 44% 62% 38% / 46% 58% 42% 54%;
    background:
      radial-gradient(ellipse at 48% 42%, rgba(255, 239, 212, 0.42), transparent 46%),
      radial-gradient(ellipse at 26% 66%, rgba(168, 135, 83, 0.17), transparent 48%),
      radial-gradient(ellipse at 78% 34%, rgba(96, 65, 41, 0.12), transparent 50%);
    filter: blur(22px);
    opacity: 0.72;
    transform: translate3d(-50%, -50%, 0) rotate(-5deg);
  }

  .video-carousel-wrapper::after {
    content: '';
    position: absolute;
    left: 50%;
    bottom: 1.15rem;
    z-index: 0;
    width: min(74vw, 440px);
    height: 42px;
    transform: translateX(-50%) rotate(-1deg);
    pointer-events: none;
    border-radius: 999px;
    background: radial-gradient(ellipse at center, rgba(72, 46, 25, 0.18), transparent 72%);
    filter: blur(12px);
    opacity: 0.58;
  }

  .video-carousel {
    --cover-card-width: min(70vw, 270px);
    position: relative;
    z-index: 2;
    display: flex;
    align-items: center;
    gap: 0;
    overflow-x: auto;
    overflow-y: visible;
    padding-block: clamp(1.4rem, 4vw, 2.2rem) 2.9rem;
    padding-inline: calc((100vw - var(--cover-card-width)) / 2);
    scroll-behavior: smooth;
    scroll-snap-type: x mandatory;
    scroll-padding-inline: calc((100vw - var(--cover-card-width)) / 2);
    -webkit-overflow-scrolling: touch;
    transform-style: preserve-3d;
    scrollbar-width: none;
  }

  .video-carousel::-webkit-scrollbar { display: none; }

  .video-card {
    flex: 0 0 var(--cover-card-width);
    height: min(102vw, 392px);
    scroll-snap-align: center;
    scroll-snap-stop: always;
    padding-inline: 0.32rem;
    transform-style: preserve-3d;
    opacity: var(--flow-opacity, 0.28);
    filter: saturate(var(--flow-saturation, 0.76)) brightness(var(--flow-brightness, 0.86));
    transition:
      opacity 360ms cubic-bezier(0.16, 1, 0.3, 1),
      filter 360ms cubic-bezier(0.16, 1, 0.3, 1);
  }

  .card-inner {
    position: relative;
    width: 100%;
    height: 100%;
    border-radius: clamp(34px, 9vw, 52px);
    overflow: hidden;
    isolation: isolate;
    clip-path: inset(0 round clamp(34px, 9vw, 52px));
    background: #1a1a1a;
    box-shadow: 0 18px 42px rgba(72, 46, 25, 0.16);
    transform:
      perspective(1400px)
      translateX(var(--flow-x, 0px))
      translateZ(var(--flow-z, -190px))
      rotateY(var(--flow-rotate, 0deg))
      scale(var(--flow-scale, 0.82));
    transform-origin: center center;
    transition:
      transform 460ms cubic-bezier(0.16, 1, 0.3, 1),
      box-shadow 460ms cubic-bezier(0.16, 1, 0.3, 1);
    cursor: pointer;
  }

  .video-card.is-active {
    z-index: 5;
  }

  .video-card.is-active .card-inner {
    box-shadow: 0 30px 72px rgba(72, 46, 25, 0.2);
  }

  .video-card.is-prev,
  .video-card.is-next {
    z-index: 3;
  }

  .video-player {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
    display: block;
    border-radius: clamp(34px, 9vw, 52px);
    clip-path: inset(0 round clamp(34px, 9vw, 52px));
    transform: translateZ(0);
  }

  .play-btn {
    position: absolute;
    inset: 0;
    margin: auto;
    z-index: 10;
    background: rgba(0, 0, 0, 0.4);
    border: none;
    border-radius: 50%;
    width: 56px;
    height: 56px;
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    opacity: 1;
    transition: opacity 300ms ease-out, background 300ms ease-out;
    -webkit-tap-highlight-color: transparent;
  }

  .insta-link {
    position: absolute;
    bottom: 1rem;
    right: 1rem;
    z-index: 11;
    background: rgba(255, 255, 255, 0.1);
    border: 0.5px solid rgba(255, 255, 255, 0.2);
    border-radius: 50%;
    width: 44px;
    height: 44px;
    display: flex;
    align-items: center;
    justify-content: center;
    text-decoration: none;
    opacity: 0.9;
    transition: opacity 300ms ease-out, background 300ms ease-out;
    -webkit-tap-highlight-color: transparent;
  }

  @media (max-width: 720px) {
    .cours-carousel { display: none !important; }

    .video-carousel {
      --cover-card-width: min(72vw, 276px);
      padding-block: 1.45rem 2.8rem;
      padding-inline: calc((100vw - var(--cover-card-width)) / 2);
    }

    .video-card {
      flex-basis: var(--cover-card-width);
      height: min(104vw, 398px);
    }

    .play-btn {
      width: 50px;
      height: 50px;
    }
  }

  @media (min-width: 721px) {
    .video-carousel-wrapper { display: none !important; }
  }
</style>

<script>
  function initCoursCarousel() {
    const carousel = document.getElementById('cours-carousel');
    if (!carousel) return;
    const slides = Array.from(carousel.querySelectorAll<HTMLElement>('.coc-slide'));
    const videos = slides.map(s => s.querySelector<HTMLVideoElement>('video')!);
    const links  = slides.map(s => s.querySelector<HTMLAnchorElement>('.coc-link')!);
    let current = 0, timer: ReturnType<typeof setInterval> | null = null;
    function activate(idx: number) {
      slides[current].classList.remove('is-active'); links[current].tabIndex = -1; videos[current]?.pause();
      current = idx;
      slides[current].classList.add('is-active'); links[current].tabIndex = 0;
      const v = videos[current];
      if (v) { v.currentTime = parseInt(slides[current].dataset.start||'0'); v.muted=true; v.play().catch(()=>{}); }
    }
    function startTimer() { if (timer) clearInterval(timer); timer = setInterval(() => activate((current+1)%slides.length), 6000); }
    let touchStartX = 0;
    carousel.addEventListener('touchstart', (e: TouchEvent) => { touchStartX = e.touches[0].clientX; }, { passive: true });
    carousel.addEventListener('touchend', (e: TouchEvent) => {
      const diff = touchStartX - e.changedTouches[0].clientX;
      if (Math.abs(diff) > 40) { activate(diff>0?(current+1)%slides.length:(current-1+slides.length)%slides.length); startTimer(); }
    }, { passive: true });
    new IntersectionObserver((entries, obs) => {
      if (!entries[0].isIntersecting) return;
      obs.disconnect();
      const first = videos[0];
      if (first) { first.currentTime = parseInt(slides[0].dataset.start||'0'); first.muted=true; first.play().catch(()=>{}); }
      startTimer();
    }, { threshold: 0.1 }).observe(carousel);
    document.addEventListener('astro:before-swap', () => { if(timer) clearInterval(timer); }, { once: true });
  }

  function initVideoCarousel() {
    const wrapper = document.getElementById('video-carousel-mobile');
    if (!wrapper) return;

    const carousel = wrapper.querySelector<HTMLElement>('.video-carousel');
    if (!carousel) return;

    const cards = Array.from(carousel.querySelectorAll<HTMLElement>('.video-card'));
    let scrollRaf = 0;
    let activeVideoIndex = -1;
    let carouselVisible = false;
    let autoTimer: ReturnType<typeof setInterval> | null = null;
    let resumeTimer: ReturnType<typeof setTimeout> | null = null;
    let userInteracting = false;
    const observers: IntersectionObserver[] = [];
    const prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
    const AUTO_SCROLL_DELAY = 3900;
    const RESUME_DELAY = 5200;

    const getCardMedia = (card: HTMLElement) => ({
      video: card.querySelector<HTMLVideoElement>('.video-player'),
      playBtn: card.querySelector<HTMLButtonElement>('.play-btn'),
    });

    const setPlayButton = (playBtn: HTMLButtonElement | null, visible: boolean) => {
      if (!playBtn) return;
      playBtn.style.opacity = visible ? '1' : '0';
      playBtn.style.pointerEvents = visible ? 'auto' : 'none';
    };

    const prepareVideo = (card: HTMLElement, video: HTMLVideoElement) => {
      video.muted = true;
      video.defaultMuted = true;
      video.playsInline = true;
      video.setAttribute('playsinline', '');
      video.setAttribute('webkit-playsinline', '');
      video.preload = 'auto';

      const src = card.dataset.src;
      if (src && !video.currentSrc) {
        video.src = src;
      }
      if (video.readyState === 0) {
        video.load();
      }
    };

    const seekToStart = (card: HTMLElement, video: HTMLVideoElement) => {
      const startTime = parseInt(card.dataset.start || '0');
      const applySeek = () => {
        try {
          if (Math.abs(video.currentTime - startTime) > 0.5) {
            video.currentTime = startTime;
          }
        } catch {}
      };

      if (video.readyState >= 1) {
        applySeek();
      } else {
        video.addEventListener('loadedmetadata', applySeek, { once: true });
      }
    };

    const pauseInactiveVideos = (activeIndex: number) => {
      cards.forEach((card, index) => {
        if (index === activeIndex) return;
        const { video, playBtn } = getCardMedia(card);
        if (!video) return;
        video.pause();
        setPlayButton(playBtn, true);
      });
    };

    const pauseAllVideos = () => {
      cards.forEach(card => {
        const { video, playBtn } = getCardMedia(card);
        if (!video) return;
        video.pause();
        setPlayButton(playBtn, true);
      });
    };

    const playActiveVideo = (index: number, reset = false) => {
      if (!carouselVisible) return;

      const card = cards[index];
      if (!card) return;

      const { video, playBtn } = getCardMedia(card);
      if (!video) return;

      pauseInactiveVideos(index);
      prepareVideo(card, video);
      if (reset) seekToStart(card, video);

      video.play()
        .then(() => setPlayButton(playBtn, false))
        .catch(() => setPlayButton(playBtn, true));
    };

    const centerCard = (index: number, behavior: ScrollBehavior = 'smooth') => {
      const card = cards[index];
      if (!card) return;
      const targetLeft = card.offsetLeft - (carousel.clientWidth - card.offsetWidth) / 2;
      carousel.scrollTo({ left: targetLeft, behavior });
    };

    const stopAutoScroll = () => {
      if (!autoTimer) return;
      clearInterval(autoTimer);
      autoTimer = null;
    };

    const startAutoScroll = () => {
      if (prefersReducedMotion || autoTimer || cards.length < 2) return;
      autoTimer = setInterval(() => {
        if (!carouselVisible || userInteracting || document.hidden) return;
        const nextIndex = activeVideoIndex < 0 ? 0 : (activeVideoIndex + 1) % cards.length;
        centerCard(nextIndex);
      }, AUTO_SCROLL_DELAY);
    };

    const pauseAutoScrollBriefly = () => {
      userInteracting = true;
      if (resumeTimer) clearTimeout(resumeTimer);
      resumeTimer = setTimeout(() => {
        userInteracting = false;
        if (carouselVisible) startAutoScroll();
      }, RESUME_DELAY);
    };

    const updateCoverflow = () => {
      scrollRaf = 0;
      const carouselRect = carousel.getBoundingClientRect();
      const center = carouselRect.left + carouselRect.width / 2;
      let activeIndex = 0;
      let closest = Number.POSITIVE_INFINITY;

      cards.forEach((card, index) => {
        const rect = card.getBoundingClientRect();
        const cardCenter = rect.left + rect.width / 2;
        const distance = Math.abs(cardCenter - center);
        if (distance < closest) {
          closest = distance;
          activeIndex = index;
        }
      });

      cards.forEach((card, index) => {
        const rect = card.getBoundingClientRect();
        const cardCenter = rect.left + rect.width / 2;
        const rawOffset = (cardCenter - center) / Math.max(rect.width, 1);
        const offset = Math.max(-2.2, Math.min(2.2, rawOffset));
        const absOffset = Math.abs(offset);
        const depth = Math.min(absOffset, 2);
        const scale = 1 - depth * 0.105;
        const z = 110 - depth * 150;
        const rotate = -offset * 25;
        const translateX = -offset * 16;
        const opacity = Math.max(0.24, 1 - absOffset * 0.34);
        const saturation = Math.max(0.76, 1 - absOffset * 0.09);
        const brightness = Math.max(0.86, 1 - absOffset * 0.045);

        card.style.setProperty('--flow-x', `${translateX.toFixed(1)}px`);
        card.style.setProperty('--flow-z', `${z.toFixed(1)}px`);
        card.style.setProperty('--flow-rotate', `${rotate.toFixed(2)}deg`);
        card.style.setProperty('--flow-scale', scale.toFixed(3));
        card.style.setProperty('--flow-opacity', opacity.toFixed(3));
        card.style.setProperty('--flow-saturation', saturation.toFixed(3));
        card.style.setProperty('--flow-brightness', brightness.toFixed(3));

        card.classList.remove('is-active', 'is-prev', 'is-next', 'is-far-left', 'is-far-right');
        if (index === activeIndex) {
          card.classList.add('is-active');
        } else if (index === activeIndex - 1) {
          card.classList.add('is-prev');
        } else if (index === activeIndex + 1) {
          card.classList.add('is-next');
        } else if (index < activeIndex) {
          card.classList.add('is-far-left');
        } else {
          card.classList.add('is-far-right');
        }
      });

      const changedActiveVideo = activeIndex !== activeVideoIndex;
      activeVideoIndex = activeIndex;
      playActiveVideo(activeIndex, changedActiveVideo);
    };

    const scheduleCoverflow = () => {
      if (scrollRaf) return;
      scrollRaf = requestAnimationFrame(updateCoverflow);
    };

    cards.forEach(card => {
      const video = card.querySelector<HTMLVideoElement>('.video-player');
      const playBtn = card.querySelector<HTMLButtonElement>('.play-btn');
      if (!video || !playBtn) return;

      const observer = new IntersectionObserver((entries) => {
        if (entries[0].isIntersecting) {
          prepareVideo(card, video);
        }
      }, { threshold: 0, rootMargin: '500px 0px' });
      observer.observe(card);
      observers.push(observer);

      playBtn.addEventListener('click', (e: Event) => {
        e.preventDefault();
        const index = cards.indexOf(card);
        activeVideoIndex = index;
        prepareVideo(card, video);
        seekToStart(card, video);
        pauseInactiveVideos(index);
        video.play()
          .then(() => setPlayButton(playBtn, false))
          .catch(() => setPlayButton(playBtn, true));
      });

      card.addEventListener('click', (e) => {
        if ((e.target as HTMLElement).closest('a, button')) return;
        pauseAutoScrollBriefly();
        card.scrollIntoView({ behavior: 'smooth', inline: 'center', block: 'nearest' });
      });

      video.addEventListener('play', () => { playBtn.style.opacity = '0'; });
      video.addEventListener('pause', () => { playBtn.style.opacity = '1'; });
      video.addEventListener('ended', () => { playBtn.style.opacity = '1'; });
    });

    const visibilityObserver = new IntersectionObserver((entries) => {
      carouselVisible = entries[0]?.isIntersecting ?? false;
      if (carouselVisible) {
        playActiveVideo(activeVideoIndex >= 0 ? activeVideoIndex : 0, true);
        startAutoScroll();
      } else {
        stopAutoScroll();
        pauseAllVideos();
      }
    }, { threshold: 0.38 });
    visibilityObserver.observe(wrapper);
    observers.push(visibilityObserver);

    const handleVisibilityChange = () => {
      if (document.hidden) {
        stopAutoScroll();
        pauseAllVideos();
      } else {
        playActiveVideo(activeVideoIndex >= 0 ? activeVideoIndex : 0);
        if (carouselVisible) startAutoScroll();
      }
    };

    const handleUserInteraction = () => pauseAutoScrollBriefly();

    carousel.addEventListener('scroll', scheduleCoverflow, { passive: true });
    carousel.addEventListener('pointerdown', handleUserInteraction, { passive: true });
    carousel.addEventListener('touchstart', handleUserInteraction, { passive: true });
    carousel.addEventListener('wheel', handleUserInteraction, { passive: true });
    carousel.addEventListener('keydown', handleUserInteraction);
    window.addEventListener('resize', scheduleCoverflow, { passive: true });
    document.addEventListener('visibilitychange', handleVisibilityChange);
    updateCoverflow();

    document.addEventListener('astro:before-swap', () => {
      if (scrollRaf) cancelAnimationFrame(scrollRaf);
      stopAutoScroll();
      if (resumeTimer) clearTimeout(resumeTimer);
      pauseAllVideos();
      observers.forEach(observer => observer.disconnect());
      carousel.removeEventListener('pointerdown', handleUserInteraction);
      carousel.removeEventListener('touchstart', handleUserInteraction);
      carousel.removeEventListener('wheel', handleUserInteraction);
      carousel.removeEventListener('keydown', handleUserInteraction);
      window.removeEventListener('resize', scheduleCoverflow);
      document.removeEventListener('visibilitychange', handleVisibilityChange);
    }, { once: true });
  }

  document.addEventListener('astro:page-load', initCoursCarousel);
  document.addEventListener('astro:page-load', initVideoCarousel);
</script>
